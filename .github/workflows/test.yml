name: test

on:
  push:

jobs:
    build:
        name: Build
        runs-on: ubuntu-latest
        steps:
        - name: test
          run: |
            echo $GITHUB_WORKFLOW
            echo $GITHUB_REPOSITORY
            echo $GITHUB_RUN_ID
